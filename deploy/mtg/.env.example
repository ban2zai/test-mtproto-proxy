# Сгенерировать секрет командой:
#   docker run --rm ghcr.io/9seconds/mtg:2 generate-secret www.google.com
# или простой hex:
#   docker run --rm ghcr.io/9seconds/mtg:2 generate-secret --hex
MTG_SECRET=ee<вставить-сгенерированный-секрет>
